#include <iostream>
#include <algorithm>
#define ll long long
using namespace std;

ll fact (ll n)
{
    ll p=1;
    for (int i=2;i<=n; ++i) p*=i;
    return p;
}

int main()
{
    ll n, a1, x, y, m, k;
    cin>>n>>a1>>x>>y>>m>>k;
    
}